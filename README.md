# Cicada-Engine
A modern, vulkan-based rendering engine

# Build instructions

## Windows
Can someone using windows handle this?

## Linux
Run `scripts/install_dependencies_linux.sh` to install all required dependencies. Build with `cmake .` and run the generated executable in `bin/CicadaEngine`.